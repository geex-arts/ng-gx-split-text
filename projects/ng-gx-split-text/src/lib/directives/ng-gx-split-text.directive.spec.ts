import { NgGxSplitTextDirective } from './ng-gx-split-text.directive';

describe('NgGxSplitTextDirective', () => {
  it('should create an instance', () => {
    const directive = new NgGxSplitTextDirective();
    expect(directive).toBeTruthy();
  });
});
